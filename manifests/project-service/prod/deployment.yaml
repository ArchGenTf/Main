apiVersion: apps/v1
kind: Deployment
metadata:
  namespace: prod
  name: archgen-prod-project-service
  labels:
    app: project-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: project-service
  template:
    metadata:
      labels:
        app: project-service
        azure.workload.identity/use: "true"
    spec:
      serviceAccountName: archgen-prod-project-service-sa
      containers:
        - name: project-service
          image: "acrarchgen.azurecr.io/project-service:latest"
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8002
              protocol: TCP
          env:
            - name: DATABASE_NAME
              valueFrom:
                secretKeyRef:
                  name: archgen-prod-project-service-secrets
                  key: DATABASE_NAME
            - name: ALLOWED_ORIGINS
              valueFrom:
                secretKeyRef:
                  name: archgen-prod-project-service-secrets
                  key: ALLOWED_ORIGINS
            - name: JWT_SECRET_KEY
              valueFrom:
                secretKeyRef:
                  name: archgen-prod-project-service-secrets
                  key: JWT_SECRET_KEY
            - name: MONGO_URI
              valueFrom:
                secretKeyRef:
                  name: archgen-prod-project-service-secrets
                  key: MONGO_URI
          volumeMounts:
            - name: secrets-store-inline
              mountPath: "/mnt/secrets-store"
              readOnly: true
          livenessProbe:

            httpGet:

              path: /healthz

              port: http

            initialDelaySeconds: 15

            periodSeconds: 20

          readinessProbe:

            httpGet:

              path: /ready

              port: http

            initialDelaySeconds: 5

            periodSeconds: 10
          resources:
            limits:
              cpu: 200m
              memory: 256Mi
            requests:
              cpu: 100m
              memory: 128Mi
      volumes:
        - name: secrets-store-inline
          csi:
            driver: secrets-store.csi.k8s.io
            readOnly: true
            volumeAttributes:
              secretProviderClass: "archgen-prod-project-service-keyvault-provider"
